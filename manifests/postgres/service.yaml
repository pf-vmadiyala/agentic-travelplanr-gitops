apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: travel-planner
  labels:
    app: postgres
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres
  clusterIP: None # Headless service for StatefulSet stable network routing
